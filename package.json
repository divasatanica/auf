{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2"
  },
  "scripts": {
    "setup": "lerna bootstrap && cd demo && yarn install",
    "dev:setup-server": "cd demo && npm run start",
    "dev:compile": "lerna run --parallel dev"
  }
}
